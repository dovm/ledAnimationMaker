

for wireConnection, add parameter of "length"
double click on the wire should open a dialig to change the length
when drawed on the screen, add the length writen in the midle of the wire
make shure it is also added to the serilaize objects for save and load


now let work on effects tab
effects are defined on a led strip, and can have nultiple effect
each effect represent a specific logic that will run an animation on a strip
be multiple things buf for now, just make the effcts tab compatible as well with the led_project


add the effects to the option of saving and loading
a file can have only layout, layout and animation or layout , animation and effects
and update file menu in effects to have the same options

fix group animation in the animation tab to match the new approch of led_project
the groups parameter (bool) define if the animation is preform only one one group and change color for each frame
or that the animation is preformed on all the groups by there index and each frame a diffrent group will be painted with color

remove load and save buttons from animation list
and remove load and save buttons from effect list

in animation tab:
    remove select tool from tool bar
    add "group button to tool bar that wil create a group on selected leds
    remove draw tool
    remove toolbar from the group list
    make the grouplist draggable so it can be reordered

in animation tab:
    add a button to duplicate an animation
in effects tab:
    double click on a effect, open the effect modal to edit it

in effect tab:
    move spectrum analyzer to a modal, and add a button on the top tool bar to open it
